# @package data
# No augmentation (default). Image + extrinsic stay clean; only the fixed
# 224x480 resize/crop is applied. This is the verified-geometry baseline.
augment_img: False
augment_bev: False
extrin_noise:
  enabled: False
  rot_std_deg: 0.0
  trans_std_m: 0.0
