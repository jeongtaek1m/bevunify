# @package data
# Image-warp augmentation only (resize/zoom/rotate/crop/flip via data.img_params;
# intrinsics are updated to match, so model geometry stays consistent).
augment_img: True
augment_bev: False
extrin_noise:
  enabled: False
  rot_std_deg: 0.0
  trans_std_m: 0.0
