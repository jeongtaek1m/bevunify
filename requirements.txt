# bevunify dependencies (host conda env, Python 3.8). Versions = what this project was verified with.
#
# Install PyTorch FIRST from the official CUDA index (not pinnable here cleanly):
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
# PointBeV sparse-conv wheel must match your CUDA toolkit:
#   pip install spconv-cu120==2.3.6
# Then:  pip install -r requirements.txt
#
# The 6 BEV-seg source repos (GaussianLSS, cross_view_transformers, LaRa,
# lift-splat-shoot, PointBeV, simple_bev) are used IN PLACE (not via pip) — see README.
# Extra build/patch steps (GaussianLSS rasterizer, PointBeV CUDA op + __future__ patch,
# LaRa WEIGHTS_PATH) are also in the README.

# --- core: Hydra spine + GaussianLSS host + data pipeline ---
hydra-core==1.3.2
omegaconf==2.3.0
pytorch-lightning==2.1.0
torchmetrics==1.5.2
numpy==1.24.1
opencv-python==4.13.0.92
Pillow==10.2.0
matplotlib==3.5.3
tqdm==4.65.2
einops==0.3.2
pyquaternion==0.9.9
shapely==1.8.5.post1
scikit-image==0.21.0
fvcore==0.1.5.post20210825
efficientnet-pytorch==0.7.1
nuscenes-devkit==1.1.11
wandb==0.15.5

# --- per-model extras ---
tensorboardX==2.6.2.2     # lift-splat-shoot
fairscale==0.4.13         # LaRa
hydra-colorlog==1.2.0     # LaRa / PointBeV (their repo configs)
rich==14.3.4              # PointBeV

# CUDA-specific (install separately, see notes above):
# torch==2.1.0+cu121
# torchvision==0.16.0+cu121
# spconv-cu120==2.3.6
